# MongoDB connection string (MongoDB Atlas or local)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/forge?retryWrites=true&w=majority

# Secret used to sign auth JWTs — set this to a long random string
JWT_SECRET=replace-this-with-a-long-random-string
